import React, { ChangeEvent } from 'react';
import { TERowCustomProps } from '../TERow';
import { TELabelCustomProps } from '../TELabel';
import { TECheckboxInputProps } from '../TECheckboxInput';
interface TECheckboxGroupButtonData {
    label: string;
    value?: string;
    key?: string;
}
interface TECheckboxGroupProps extends TERowCustomProps, TELabelCustomProps, Omit<TECheckboxInputProps, 'onChange'> {
    labelForKey: string;
    onChange(values: string[], event: ChangeEvent<HTMLInputElement>): void;
    inputRowSize?: string;
    checkedValues: string[];
    buttonArray: (string | TECheckboxGroupButtonData)[];
    scrolling?: boolean;
}
export declare const TECheckboxGroup: React.FC<TECheckboxGroupProps>;
export {};
